# TODO List

- Add arrow for calendar button
- Click on date modal (calendar)
- Hover over todo shows more details on calender
- Fix navbar on mobile (theme icon)
- Change navbar to be dropdown style on mobile with SVG triangle (animated)
- Archiving todo should still keep track in completion rate
- Category breakdown is off, it will show #2 with 0% but then unarchiving a todo will show 2 (200%), use 100% always and break it up based on the category count
    - example 2 work and 1 health would be 66% and 33% (even if they are all archived, partially archived or unarchived)
- No archive toast on board page
- Multi selection for bulk updates on board page
- Add toast (Moved task to 'In-Progress' with undo)
    - should have (Moved x tasks to 'Done') for multiple tasks
- Custom error 404 page and 500 page
